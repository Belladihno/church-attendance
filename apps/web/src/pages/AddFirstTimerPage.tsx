import { PageHeader, EmptyState } from '../components/ui/PageHeader';
export function AddFirstTimerPage() {
  return (
    <div>
      <PageHeader title="Add first timer" />
      <EmptyState message="AddFirstTimer form — Step 14.7" />
    </div>
  );
}
