import { PageHeader, EmptyState } from '../components/ui/PageHeader';
export function MemberDetailPage() {
  return (
    <div>
      <PageHeader title="Member detail" />
      <EmptyState message="Profile + mini attendance history — Step 14.5" />
    </div>
  );
}
