import { PageHeader, EmptyState } from '../components/ui/PageHeader';
export function FirstTimersListPage() {
  return (
    <div>
      <PageHeader title="First timers" />
      <EmptyState message="First timers list + convert — Step 14.7" />
    </div>
  );
}
